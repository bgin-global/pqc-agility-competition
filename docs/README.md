# docs/

Reference documentation that is about the repository itself rather than about the competition's content.

| File | What |
|---|---|
| `SIGNING.md` | How to produce commits and tags this repository accepts (signing key, DCO, hooks, troubleshooting) |
| `CONFIDENTIALITY.md` | What is held back from the public record, the four tests that decide it, and the gate material passes through to be released |
| `AUTORESEARCH.md` | Proposal (draft v0, for W2/W3): calibration tracks as open benchmarks — manifest, editable paths, trusted validator, public attributed notes, promotion = reproduction; the boundary with the prize |
| [`../harness/README.md`](../harness/README.md) | The calibration harness: the dual-agent instance that runs the tracks — lineage, the two halves, how to run a round, known upstream defects |
| `SECURITY_MODEL.md` | Why every action carries a signature: layers, baseline controls, teams, org-owner checklist, ceremonies, threat coverage |

Competition content lives in the topical directories: `evaluation/`, `testbed/`, `workshops/`, `decisions/`, `resources/`.
